# 바이너리 인덱스 트리는 2진법 인덱스 구조를 활용해 구간 합 문제를 효과적으로 해결해 줄 수 있는 자료구조를 말한다.
# 팬윅트리 라고도 합니다.
import sys

N, M, K = map(int, sys.stdin.readline().split())
graph = [int(sys.stdin.readline()) for _ in range(N)]

for _ in range(K):
    a, b, c = map(int, sys.stdin.readline().split())






